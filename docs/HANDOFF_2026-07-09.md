# Handoff - Phase 10g Re-acquisition + Blocks Lane (2026-07-09 -> 07-10 overnight)

**Written:** 2026-07-10 ~03:45 local host time.
**Repo:** `/home/frosty40/overworld`
**Branch:** `spin-persistence` (commit pending at write time; this doc is part
of the session commit).
**Previous handoff:** `docs/HANDOFF_PUSH_BOUNDARY_2026-07-08.md`.
**Status:** Phase 10g controlled-complete (Protocol A PASS; Protocol B
boundary identified). Blocks/BlockN asset lane opened with first aggregate +
visual results. GPU is FREE (user planned to load leanstral overnight).

## Read First

1. `docs/ANCHOR_REACQ_PLAN.md` — Phase 10g plan **including Pilot Amendments
   10g.1 and 10g.2** (two admission gates were proposed and refuted in pilots;
   the amendments carry the evidence).
2. `docs/ANCHOR_REACQ_RESULTS.md` — Phase 10g verdict: PARTIAL, boundary
   re-scoped.
3. `docs/BLOCKS_PROXY_RESULTS.md` — first result on the LegoGen
   primitive-architecture asset distribution.
4. `bench_out/anchor_reacq/pan180_scene0_seed1235/index.html` — the MP4
   packet showing the honest `nomatch` at 180-degree loop closure.
5. `bench_out/blocks_proxy/visual_review/index.html` — blocks visual packet,
   including the wrong-pose distribution-collapse case.

## What Was Actually Proven (this session)

### Phase 10g (biome condition, `bench_assets`)

Promoted algorithm (10g.2): content-ranked multi-hypothesis retrieval inside a
spacing-aware pose-prior window; resp/shift gates; cross-micro `admit_consist`;
pose admission final gate; explicit `nomatch` fail-closed state.

- 4x4 sweep, canary `+2.94 +/- 1.07 dB 16/16` (exact Phase 10f replication):
  `anchor4_full_reacq_pose02 = +3.75 +/- 2.66 dB` vs `atlas_nowb`, **16/16
  wins** (incumbent: `+3.93 +/- 3.34`, 14/16) — no regression, tighter spread.
- Far control: **0% accept, 16/16 explicit nomatch, post-window delta exactly
  +0.00** — structural no-teleport guarantee (wrong-pose keyframes are never
  scored).
- 180-degree pan (scene 0/seed 1235): incumbent `reject=empty` replicated;
  reacq returns honest `nomatch` (best in-window resp 0.031 < 0.05). MP4
  frames prove the mechanism: **the returned content has fully collapsed —
  the remembered place no longer exists at closure time.** Not a retrieval
  failure; a content-drift wall. Evidence stills in
  `docs/permanence_assets/reacq_pan180_scene0_*.png`.

### Two refuted gates (documented negatives, do not resurrect)

1. **10g.1 pose-plausibility hard gate** (`|dx/px_per_yaw - drift| <= 1.5`):
   refuted in pilot 2 — with real store calibration (ppy 31.4) the true match
   (residual 1.39) and far alias (1.44) are indistinguishable; px-per-yaw gain
   is context-unstable and content drift enters the residual exactly like
   aliasing. Kept as logged diagnostic only.
2. **Whole-store content-ranked search without a pose prior** (initial 10g):
   pilot 1 showed the far control anchoring coherent wrong-pose matches.
   Response ranking cannot separate truth from symmetric aliases.

### Blocks/BlockN lane (`mechanics_proxy`, new asset distribution)

- `bench_assets_blocks/` (LegoGen commit `6c147f5`, 4 B/W surreal
  classical-architecture scenes + landmarks) probed 4x3: in-run canary 12/12;
  `anchor4_full(_pose02) = +3.28 +/- 2.83 dB` vs `atlas_nowb`, 12/12
  (`+5.84` vs revisit). The memory stack transfers to the OOD primitive world.
- **Pose aliasing is worse on primitive worlds**: one far-pose control matched
  wrong-pose content at resp 0.910 / dx 0.1 px and cost -5.5 dB. Response-only
  admission is provably insufficient here.
- **Wrong-pose anchoring can collapse the rollout out of distribution**: the
  visual packet's alias case shows the model leaving the B/W world for an
  FPS-HUD hallucination after a wrong-pose anchor. Failure-anthology keeper:
  `bench_out/blocks_proxy/visual_review/seed_02_1236_blocks_alias/`.
- v2 assets from the user's same-day BlockN push (`newjordan/Blocks` branch
  `feat/action-plan-simple-to-complex` commit `9370714`) generated via the new
  front-door `service.build()` workflow: `bench_assets_blockn/` (+ sunset
  variants); BlockN's own test suite 71/71 green. **Not yet probed.**

## Code Changes (uncommitted at write time; part of this session's commit)

`examples/anchor.py`: `AnchorStore.query_k`, `pose_window`, `reacquire`,
`reconstruct_temporal_from_keyframe`, `reacq_reconstruct`,
`calibrate_px_per_yaw` + `pose_plausible` (diagnostics only).
`examples/anchor_probe.py` / `anchor_visual.py` (byte-identical shared
copies) / `anchor_pan_video.py`: reacq arms `anchor4_full_reacq_pose02(_far)`,
`_append_reacq`, `admit_consist` gate, `nomatch` vocabulary, `--reacq-k`,
diagnostics in console + manifest. `examples/test_anchor.py`: 30 CPU tests
(16 pre-10g byte-unmodified + 14 new). All Opus-4.8-agent implemented, three
review rounds, verified each round:
`uv run --dev pytest examples/test_anchor.py -q` -> 30 passed;
`py_compile` clean; `git diff --check` clean.

## Do Not Cite These As Results

- `bench_out/anchor_reacq/pilot*.csv` — scouts (n=1), process artifacts.
- `bench_out/anchor_reacq/pan180_scene1_seed1236/` — OOM-killed mid-run
  (kernel oom-killer, 54 GB RSS at 455 away steps). Harness boundary.
- `bench_out/anchor_reacq/pan180_scene2_seed1237/` — content-rotation
  pathology (30.5 deg at cap 800); scene cannot express the condition.
- The blocks visual packet reruns are separate trials from the aggregate CSV
  (run-to-run nondeterminism); packet is review, CSV is the claim.

## Standing Rules Still Binding

KV write-back stays dead (both back-door negatives + Phase 10f/g gates all
consistent). Paired within-trial comparisons only. `atlas_nowb` canary in
every sweep. New work must change representation or claim. `bench_out/` is
gitignored — copy keepers to `docs/permanence_assets/`.

## Best Next Moves

1. **Mid-pan cadence anchoring** (the next representation change): anchor
   during the pan while content still registers, bounding drift before loop
   closure. The 180-degree wall is content death, not retrieval — memory must
   enter earlier. Reuse the cadence plumbing from Phase 10c-d arms.
2. **Harness: streamed MP4 writing** in `anchor_pan_video.py` (fixes the
   455-step OOM) + pick a replacement third pan case (scene 3 or new seeds;
   verify content rotation reaches 180 before citing).
3. **BlockN probe**: run the mechanics_proxy sweep on `bench_assets_blockn/`
   (command pattern in `docs/BLOCKS_PROXY_RESULTS.md`; run from a clean
   worktree if the main tree is mid-edit).
4. **Sync atlas rig**: `newjordan/Blocks` GitHub can lag the atlas (Windows,
   E:\block) working copy; the user pushes on request.
5. Gauntlet/demo path (NORTHSTAR Phases 11-12) now has two asset
   distributions and a promoted re-acquisition gate to fold in.

## Environment Notes

- GPU: GB10 (spark-2949), 121 GB unified; `nvidia-smi` memory fields read N/A
  and util can read 96% while idle — trust `--query-compute-apps`.
- Worktree `/home/frosty40/overworld-wt-blocks` (HEAD `1af98df`) was used to
  run committed-code GPU jobs while agents edited the main tree — reusable
  pattern, remove when stale.
- `--run-label` accepts only `new_experiment|scout|mechanics_proxy`.
