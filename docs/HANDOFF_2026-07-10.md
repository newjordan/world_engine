# Handoff + Agent Task List - Phase 10h pilot chain + BlockN v2 (2026-07-10)

**Written:** 2026-07-10, midday session wrap.
**Repo:** `/home/frosty40/overworld`, branch `spin-persistence`, committed and
pushed at `28fb15e` (main tree clean).
**Previous handoff:** `docs/HANDOFF_2026-07-09.md`.
**Standing goal sheet:** `docs/NORTHSTAR.md`. **Phase 10h condition source:**
`docs/ANCHOR_CADENCE_PLAN.md`.

## State at wrap

Committed in `28fb15e` (all tests 43/43, reviewed):

1. **MP4 streaming fix** — `anchor_pan_video.py` now spills frames to a PNG
   `FrameSpool` and streams both MP4 writers. The 455-step OOM (54 GB RSS,
   kernel kill) is gone; Protocol B runs are safe even with leanstral
   co-resident. `--keep-frames` retains the spill.
2. **Phase 10h cadence arms** — `anchor4_full_reacq_k4_pose02(_far)` and `k8`
   pilot variants registered; `_cadence` token parse; shared
   `_needs_final_anchor` (byte-identical `anchor_probe.py`/`anchor_visual.py`
   mirrors, locked by a sync test); return-leg cadence in the pan-video runner
   with per-attempt `cadence_attempts` diagnostics flowing into the manifest.
3. **Phase 10h plan** — `docs/ANCHOR_CADENCE_PLAN.md` (question, arms, gates,
   pilot discipline, runbook).

Run overnight/this morning (bench_out is gitignored — results live on this
host):

- **BlockN v2 probe COMPLETE** (`bench_out/blockn_proxy/`, run from clean
  worktree at `16dde6d`): canary `+3.36 +/- 2.25` 12/12;
  `anchor4_full(_pose02) = +1.24 +/- 3.23 dB` vs `atlas_nowb`, **only 6/12**
  (v1 blocks: +3.28, 12/12) — much weaker transfer than v1. Anomalies to
  resolve before citing: one accepted anchor at mean `|dx| 97.6 px` (disease
  band, seed_01/1234); `nan` mean `|dx|` in the far arm aggregate; far accept
  77%.

## In flight at wrap (check these FIRST)

- **Protocol A wiring scout** (Phase 10h): running at wrap.
  `bench_out/anchor_cadence/pilot.csv` + `pilot.log`, run label `scout`, 1x1,
  arms `revisit,atlas_nowb,anchor4_full_reacq_pose02,anchor4_full_reacq_k4_pose02,anchor4_full_reacq_k8_pose02,anchor4_full_reacq_k4_pose02_far`.
  If the log ends mid-run, rerun the command (it is cheap, ~10 min).
- **BlockN analysis draft**: an agent was drafting
  `docs/BLOCKN_PROXY_RESULTS.md` from the CSV. If present it is DRAFT,
  unreviewed — do not cite until Task 2 below is done. If absent, do Task 2
  from scratch.

## Task list (ordered; each task states its gate)

### Task 1 — Review the 10h wiring scout, then launch the scene-0 pan pilot
1. Read `bench_out/anchor_cadence/pilot.log`. Gates (n=1 scout, wiring only):
   canary positive; `reacq_k4`/`k8` deltas in the same range as the incumbent
   `anchor4_full_reacq_pose02` (no blowup, no zero-accept); `_far` arm 0%
   accept with `nomatch` rejects. Any gate failure = wiring bug: fix, rerun
   scout, do NOT proceed.
2. On green: launch the pilot (GPU, ~1-2 h, run from the clean main tree at
   `28fb15e`):
   ```bash
   uv run --dev python examples/anchor_pan_video.py \
     --condition-source docs/ANCHOR_CADENCE_PLAN.md \
     --arms revisit,anchor4_full_reacq_pose02,anchor4_full_reacq_k4_pose02,anchor4_full_reacq_k8_pose02 \
     --target-deg 180 --yaw-mag 4.0 --cap 800 \
     --scene 0 --seed 1235 \
     --out bench_out/anchor_cadence/pilot_pan180_scene0_seed1235
   ```
   (revisit first locks the away steps; expect 217 / 180.16 deg. This is a
   `visual_review` pilot for the 10h mechanism — the plan's Protocol B proper
   comes after promotion.)
3. Review `manifest.json`: for each cadence arm read `cadence_attempts` — the
   money numbers are (a) accept rate and dx health along the return leg,
   (b) the `closure=true` attempt's best in-window resp vs the incumbent's
   0.031, (c) post16 vs the incumbent's 11.60. Decide per the plan's gates:
   promote one cadence (C=4 or C=8) into the Protocol A sweep, or amend
   `docs/ANCHOR_CADENCE_PLAN.md` with the refutation evidence (10g.1/10g.2
   amendments are the template). Do not tune resp_min/window/thresholds.

### Task 2 — Verify + finish the BlockN v2 results doc
1. Review/complete `docs/BLOCKN_PROXY_RESULTS.md` against
   `bench_out/blockn_proxy/anchor_blockn.csv` (5795 rows) in the exact format
   of `docs/BLOCKS_PROXY_RESULTS.md`. Required content: per-scene x per-seed
   paired table; which 6 trials lose vs `atlas_nowb` and by how much; dissect
   the 97.6 px disease-band accept (seed_01/1234); root-cause the `nan` mean
   `|dx|` in `anchor4_full_far` (harness aggregation artifact vs real); state
   plainly that v2 transfer is weaker than v1 and which scenes drive it.
2. Boundary language: `mechanics_proxy`, 12 paired trials, no pooling with
   biome or v1-blocks aggregates.
3. Commit the doc (+ index/report artifacts copied to
   `docs/permanence_assets/` if any are keepers).

### Task 3 — Validate a replacement third pan case (cheap, GPU)
Scene 2/seed 1237 cannot express 180 deg (30.5 deg at cap 800, twice
replicated). Find a case that can: run `anchor_pan_video.py` with
`--arms revisit --cap 800 --yaw-mag 4.0 --target-deg 180` over candidates
(scene 3/seed 1238 first, then alternate seeds on scenes 0/1/3), read
`away_deg` from each manifest. A case is valid iff measured content rotation
reaches ~170+ deg. Record the chosen case in `docs/ANCHOR_CADENCE_PLAN.md`
(Protocol B case list). Streaming fix makes this safe at cap 800.

### Task 4 — Phase 10h Protocol B round (after Task 1 promotes a cadence)
Three valid cases: scene 0/seed 1235 (locked 217), scene 1/seed 1236 (455
steps — now OOM-safe), Task 3's case. Full arm set per the plan runbook
(incl. the `_far` cadence control on at least one case). Gates and verdict
taxonomy are in `docs/ANCHOR_CADENCE_PLAN.md`. Copy evidence stills of any
headline result to `docs/permanence_assets/`.

### Task 5 — Phase 10h Protocol A sweep (only after Tasks 1+4 look sane)
4 scenes x 4 seeds, command in the plan runbook. Gates: canary +/- 1 dB of
+2.94; no regression vs incumbent; far control 0% accept, +0.00 side effects.
Then write `docs/ANCHOR_CADENCE_RESULTS.md` (10g results doc is the
template), commit, and update the handoff chain.

### Housekeeping (any agent, anytime)
- Worktree `/home/frosty40/overworld-wt-blocks` is detached at `16dde6d`;
  advance to `28fb15e` if used for GPU runs, or `git worktree remove` when
  stale.
- The Claude memory file for this project should be updated after Task 1's
  verdict (it currently records the pilot chain as pending).

## Standing rules (unchanged, binding)
KV write-back stays dead. Paired within-trial comparisons only. `atlas_nowb`
canary in every sweep. New work must change representation or claim — no
threshold tweaks against evidence (the 10g stop rule on the 180-deg wall
applies to cadence work too: if the pilot shows content death still wins,
document it, don't tune). `bench_out/` is gitignored — keepers go to
`docs/permanence_assets/`. `--run-label` accepts only
`new_experiment|scout|mechanics_proxy`.

## Environment
- GB10 (spark-2949), 121 GB unified. **leanstral (vLLM) holds ~58 GB** — check
  `nvidia-smi --query-compute-apps=pid,process_name,used_memory --format=csv`
  before launching GPU work; probe sweeps and (post-fix) pan videos fit
  alongside it; do not run two heavy rollouts concurrently.
- `nvidia-smi` memory fields read N/A and util can read 96% while idle —
  trust `--query-compute-apps`.
- Run GPU jobs for citable results from committed code (clean main tree at
  `28fb15e` qualifies; worktree pattern if the tree is mid-edit).
